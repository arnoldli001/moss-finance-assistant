# 版本控制与本地环境
.git
.gitignore
.venv
venv/

# Python 产物
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/

# 运行时数据与生成物（挂卷，不进镜像）
data/
output/
*.db

# 本地敏感配置（运行时通过 env_file / -e 注入）
.env
.env.*

# 文档与测试（镜像内不需要）
docs/
tests/

# CI/Docker 自身
Dockerfile
docker-compose.yml
.dockerignore
.github/
